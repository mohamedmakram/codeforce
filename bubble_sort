def bubble_sort(L):
  swap = False
  while not swap:
    swap = True
    for i in range(1, len(L)):
      if L[i-1] > L[i]:
        swap = False
        temp = L[i]
        L[i] = L[i-1]
        L[i-1] = temp

to_sort = [20,5,10,3,24,15,2,7,9]
print(bubble_sort(to_sort))
print(to_sort)
