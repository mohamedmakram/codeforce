def selection_sort(L):
  suffix = 0
  while suffix != len(L):
    for i in range(suffix,len(L)):
      if L[i] < L[suffix]:
        L[suffix], L[i] = L[i], L[suffix]
    suffix += 1
to_sort = [20,5,10,3,24,15,2,7,9]
X = selection_sort(to_sort)
print(X)
print(to_sort)
